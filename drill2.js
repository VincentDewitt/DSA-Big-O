//even or odd

function isEven(value) {
    if (value % 2 == 0) {
      return true;
    } else {
      return false;
    }
  }

  // 0(1) because number of operations does not change no matter the input